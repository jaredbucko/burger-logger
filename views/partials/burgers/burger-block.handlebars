<h3>
<li>
	{{burger_name}}
	
	<button class="change-devoured waves-effect waves-light btn" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat{{else}}Un-Eat{{/if}}
	</button>

	<button class="delete-burger waves-effect waves-light btn" data-id="{{id}}">
		Delete
	</button>

</li>
</h3>

